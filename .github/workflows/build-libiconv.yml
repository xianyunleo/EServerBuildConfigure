name: Build and Release libiconv (macOS)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. libiconv-1.18)"
        required: true
  push:
    tags:
      - 'libiconv-*'

jobs:
  build_and_release:
    name: Build and Release libiconv (macOS)
    runs-on: macos-latest
    env:
      PREFIX: /Applications/EServer/Library/libiconv
      LIBICONV_VERSION: 1.18
      MACOSX_DEPLOYMENT_TARGET: 11.0

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Build libiconv
        run: |
          chmod +x mac_arm64/base/libiconv.sh
          ./mac_arm64/base/libiconv.sh

      - name: Debug workspace
        run: |
          echo "===== PWD ====="
          pwd
          
          echo "===== ROOT LS ====="
          ls -la
          
          echo "===== FULL TREE ====="
          find . -maxdepth 3 -type f -print

      - name: Show file info
        run: |
          FILE="$PREFIX/lib/libiconv.dylib"
          echo "=== file architectures ==="
          lipo -info "$FILE"
          echo "=== file minimum macOS version ==="
          otool -l "$FILE" | grep -A 3 LC_BUILD_VERSION
          echo "=== file dependencies ==="
          otool -L "$FILE"

      - name: Set artifact name
        run: echo "ARTIFACT_NAME=libiconv-macos-${LIBICONV_VERSION}.tar.gz" >> $GITHUB_ENV

      - name: Package artifact
        run: |
          tar -czf "$ARTIFACT_NAME"  -C "$(dirname "$PREFIX")" "$(basename "$PREFIX")"

      - name: Upload release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
          files: ${{ env.ARTIFACT_NAME }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
