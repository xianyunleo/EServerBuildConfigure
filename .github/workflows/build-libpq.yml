name: Build and Release libpq (macOS)

on:
  workflow_dispatch:   # 手动触发
  push:
    tags:
      - 'libpq-*'

jobs:
  build_and_release:
    name: Build and Release libpq (macOS)
    runs-on: macos-latest
    env:
      PREFIX: /Applications/EServer/Library/libpq
      POSTGRESQL_VERSION: 17.2
      MACOSX_DEPLOYMENT_TARGET: 11.0

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Build libpq
        run: |
          chmod +x mac_arm64/base/libpq.sh
          ./mac_arm64/base/libpq.sh

      - name: Show file info
        run: |
          FILE="$PREFIX/lib/libpq.dylib"
          echo "=== file architectures ==="
          lipo -info "$FILE"
          echo "=== file minimum macOS version ==="
          otool -l "$FILE" | grep -A 3 LC_BUILD_VERSION
          echo "=== file dependencies ==="
          otool -L "$FILE"

      - name: Package artifact
        run: |
          tar -czf libpq-macos.tar.gz -C "$(dirname "$PREFIX")" "$(basename "$PREFIX")"

      - name: Upload release
        uses: softprops/action-gh-release@v2
        with:
          files: libpq-macos.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
